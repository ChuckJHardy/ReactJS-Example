'use strict';

var React = require('react');

module.exports = React.createClass({
  displayName: 'Deregister',

  render: function() {
    return (
      <div>
        Deregister
      </div>
    );
  }
});
