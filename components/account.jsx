'use strict';

var React = require('react');

module.exports = React.createClass({
  displayName: 'Account',

  render: function() {
    return (
      <div>
        Account
      </div>
    );
  }
});
